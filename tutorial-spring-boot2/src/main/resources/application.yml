aplicacion:
  nombre: Tutorial Spring Boot
   
server:
  port: 9877
  
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pruebas?useSSL=false
    username: root
    password: 
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL57Dialect
    hibernate:
      ddl-auto: create-drop
      
logging:
  level:
    org:
      hibernate:
        SQL: debug